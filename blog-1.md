## How do Generics allow you to build reusable components and functions that stay strictly typed regardless of the data structures passed in?

Generice gives us to write reuseable code where type doesn't lose. We wrote a placeholder type. it prevent unexptcted error. generics gives flexibility like any with type sefty.